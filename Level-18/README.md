# OverTheWire Bandit: Level 18

### Walkthrough
The 'Shell Jail' level. The server kicked me out immediately on login. I realized the .bashrc was modified. I bypassed it by sending the 'cat readme' command directly through the SSH connection string.

### What I Learned
* SSH Command Execution. I learned you can run commands on a remote server without opening an interactive shell.
